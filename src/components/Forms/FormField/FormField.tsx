import { useEffect } from "react";

const FormField = () => {
    useEffect(() => {

    }, []);

    return (
        <div data-formId={}>
            {children}
        </div>    
    );
};

export default FormField;